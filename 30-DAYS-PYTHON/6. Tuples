'''A tuple is a collection of different data types which is ordered and unchangeable (immutable). Tuples are written with round brackets, (). Once a tuple is created, we cannot change its values. We cannot use add, insert, remove methods in a tuple because it is not modifiable (mutable). Unlike list, tuple has few methods. Methods related to tuples:

tuple(): to create an empty tuple
count(): to count the number of a specified item in a tuple
index(): to find the index of a specified item in a tuple
+ operator: to join two or more tuples and to create a new tuple'''

# EMPTY TUPLE
empty_tuple = ()
# or using the tuple constructor
emtpy_tuple = tuple()

# TUPLE WITH INITIAL VALUES
tpl = ('item1', 'item2', 'item3')
#index num   0          1        2        3
fruits = ('banana', 'orange', 'mango', 'lemon')

# TUPLE LENGTH
tpl = ('item1', 'item2', 'item3')
len(tpl)

# ACCESSING TUPLES (POSITIVE INDEX)
tpl = ('item1', 'item2', 'item3')
first_item = tpl[0]
second_item = tpl[1]

fruits = ('banana', 'orange', 'mango', 'lemon')
first_fruit = fruits[0]
second_fruit = fruits[1]
last_index = len(fruits) - 1
last_fruit = fruits[last_index]

# ACCESSING TUPLES (NEGATIVE INDEX)
fruits = ('banana', 'orange', 'mango', 'lemon')
first_fruit = fruits[-4]
second_fruit = fruits[-3]
last_fruit = fruits[-1]

# SLICING TUPLES
tpl = ('item1', 'item2', 'item3', 'item4')
all_items = tpl[0:4]
all_items = tpl[0:]
middle_two_items = tpl[0:3]

fruits = ('banana', 'orange', 'mango', 'lemon')
all_fruits = fruits[0:4]    # all items
all_fruits= fruits[0:]      # all items
orange_mango = fruits[1:3]  # doesn't include item at index 3
orange_to_the_rest = fruits[1:]

# NEGATIVE INDEX
tpl = ('item1', 'item2', 'item3', 'item4')
all_items = tpl[-4:] #left to right pa rin ang pagbasa kahit negative index slicing
midlde_two_items = tpl[-4:-1]

fruits = ('banana', 'orange', 'mango', 'lemon')
all_fruits = fruits[-4:]    # all items
orange_mango = fruits[-3:-1]  # doesn't include item at index 3
orange_to_the_rest = fruits[-3:]

# CHANGING TUPLES TO LISTS
tpl = ('item1', 'item2', 'item3', 'item4')
lst = list(tpl)

fruits = ('banana', 'orange', 'mango', 'lemon')
fruits = list(fruits) # conversion of tuples to list
fruits[0] = 'apple'
print(fruits)
fruits = tuple(fruits) # conversion of list to tuples
print(fruits)

# CHECKING AN ITEM IN A TUPLE
tpl = ('item1', 'item2', 'item3', 'item4')
'item2' in tpl # TRUE

fruits = ('banana', 'orange', 'mango', 'lemon')
print('orange' in fruits)
print('apple' in fruits)
#fruits[0] = 'apple' # error kasi di namomodify ang tuples

# JOINING TUPLES
tpl1 = ('item1', 'item2', 'item3')
tpl2 = ('item4', 'item5', 'item6')
tpl3 = tpl1 + tpl2

fruits = ('banana', 'orange', 'mango', 'lemon')
vegetables = ('Tomato', 'Potato', 'Cabbage','Onion', 'Carrot')
fruits_and_vegetables = fruits + vegetables

# DELETING TUPLES
# It is not possible to remove a single item in a tuple but it is possible to delete the tuple itself using del.
tpl1 = ('item1', 'item2', 'item3')
del tpl1

fruits = ('banana', 'orange', 'mango', 'lemon')
del fruits




# =================== EXERCISE ===================

family = ()
family = ('kath', 'kevin')
brothers = ('kevin', 'nino', 'nix', 'nico')
sisters = ('kath', 'arni', 'rochelle')
siblings = brothers + sisters
num_of_siblings = len(siblings)

fruits = ('banana', 'orange', 'mango', 'lemon')
vegetables = ('carrots', 'malunggay', 'cabbage', 'lettuce')
animal = ('dogs', 'cats', 'tiger', 'eagle')
food_stuff_tp = fruits + vegetables + animal
food_stuff_lt = list(food_stuff_tp)
print(food_stuff_lt[5:])
print(food_stuff_lt[4:10])
del food_stuff_tp

fruits = ('banana', 'orange', 'mango', 'lemon')
print('banana' in fruits)
print('apple' in fruits)

nordic_countries = ('Denmark', 'Finland','Iceland', 'Norway', 'Sweden')
print('Estonia' in nordic_countries)
print('Iceland' in nordic_countries)









